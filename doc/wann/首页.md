
# ui:
RefreshConfiguration.copyAncestor( // 下拉触发二楼距离

    child：SmartRefresher(
        child: CustomScrollView(  // 为了做成滑动的时候头部的变化
            slivers[
                SliverToBoxAdapter(),
                SliverAppBar()
            ]
        )
    )

)